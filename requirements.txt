# Pipeline & optional web backend
# SAM3 currently imports pkg_resources; setuptools 81+ warns and 82+ removes it.
setuptools<81
einops
pycocotools
psutil
# PaddlePaddle 2.x requires protobuf<=3.20.2 on Windows.
protobuf>=3.1.0,<=3.20.2; platform_system == "Windows"
pyyaml
opencv-python-headless
numpy
Pillow
scikit-image
requests
fastapi
uvicorn[standard]

# Text OCR (local, default)
pytesseract

# Optional: PaddleOCR (better for mixed Chinese/English text). Set ocr.engine: "paddleocr" in config.
# Use compatible PaddleOCR 2.x + PaddlePaddle 2.x:
#   pip install "paddleocr>=2.8.0,<3.0.0" "paddlepaddle>=2.6.1,<3.0.0"

# Optional: formula recognition (LaTeX). Install for full text pipeline.
# pip install pix2text   # and onnxruntime or onnxruntime-gpu for GPU
# Optional: background removal (RMBG). Install for IconPictureProcessor.
# pip install onnxruntime  # or onnxruntime-gpu
# PyTorch and SAM3: install separately; see README (PyTorch + SAM3 source).
