# Pipeline & optional web backend
# SAM3 currently imports pkg_resources; setuptools 81+ warns and 82+ removes it.
setuptools<81
einops
pycocotools
psutil
# SAM3/PyTorch attention stack may import Triton. Use platform markers because
# official Triton wheels are Linux-only; native Windows users can try triton-windows.
triton; platform_system == "Linux"
triton-windows; platform_system == "Windows"
# PaddleOCR 3.x/PP-OCRv6 and ONNX Runtime work with modern protobuf.
protobuf>=4.25.8
pyyaml
# Keep the scientific/CV stack on NumPy 1.26.x for PaddlePaddle/Windows compatibility.
# OpenCV 4.13+, SciPy 1.18+ and tifffile 2026+ require NumPy 2.x, so pin older
# compatible releases when the project environment stays on NumPy 1.26.4.
numpy>=1.26.4,<2.0.0
opencv-python==4.11.0.86
opencv-python-headless==4.11.0.86
scipy>=1.13.1,<1.18.0
tifffile>=2024.9.20,<2026.0.0
Pillow
scikit-image>=0.24.0,<0.26.0
requests
fastapi
uvicorn[standard]

# Text OCR (local, default)
pytesseract

# PaddleOCR / PP-OCRv6 (better for mixed Chinese/English text). Set ocr.engine: "paddleocr" in config.
paddleocr>=3.7.0,<4.0.0
paddlepaddle>=3.0.0,<3.3.0

# Optional: formula recognition (LaTeX). Install for full text pipeline.
pix2text

# RMBG background removal and setup script dependencies.
# Use onnxruntime-gpu instead if your deployment needs GPU ONNX Runtime.
onnxruntime
modelscope

# PyTorch and SAM3 are installed separately; see README and scripts/setup_sam3.sh.
